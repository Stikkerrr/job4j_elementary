package ru.job4j.condition;

public class Max {
	
	/**
	 * The method returns the greatest of the two number.
	 * @param left first number
	 * @param right second number
	 */
	public static int max(int left, int right) {
		return left > right ? left : right;
	}
}
